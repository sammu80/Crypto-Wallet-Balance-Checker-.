async function getBalance() {
    const address = document.getElementById('walletAddress').value;
    const balanceText = document.getElementById('balanceText');

    if (!ethers.utils.isAddress(address)) {
        alert("Please enter a valid Ethereum address!");
        return;
    }

    balanceText.innerText = "Fetching...";

    try {
        // Hum public provider use kar rahe hain (Cloudflare or Ankr)
        const provider = new ethers.providers.JsonProviderttps://cloudflare-eth.com");
        // Balance fetch karna (Wei mein aata h
        const balance = await provider.ge
        // Wei ko ETH mein conv
        const ethBalance = ethers.utils.formatEther(
        balanceText.innerText = `Balance: ${parseat(ealan.toed(4)TH`;
    } catch (error) {
        console.error(erro
        balance
